import type { Article, CategoryMeta } from "./articles";

// English articles are not yet available — structure reserved for future rollout.
// When adding an article, mirror the zh entry in src/lib/articles.ts.

export const CATEGORIES_EN: CategoryMeta[] = [];
export const ARTICLES_EN: Article[] = [];
